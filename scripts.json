{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run -c tsconfig.json --allow-net --allow-read --unstable server.ts",
      "desc": "run my server.ts file"
    }
  }
}